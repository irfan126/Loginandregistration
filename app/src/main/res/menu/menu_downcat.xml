<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".AddCategoryActivity">

    <!-- Search Widget -->


    <item
        android:id="@+id/search"
        android:title="Search"
        android:icon="@mipmap/ic_search_black_24dp"
        app:showAsAction="collapseActionView|ifRoom"
        app:actionViewClass="android.support.v7.widget.SearchView"/>

    <!-- Information/ will display ifroom -->
    <item android:id="@+id/action_info"
        android:icon="@mipmap/ic_info_outline_black_24dp"
        android:title="@string/action_info"
        app:showAsAction="ifRoom"/>
</menu>